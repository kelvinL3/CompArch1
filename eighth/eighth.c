#include <stdio.h>
#include <stdlib.h>
#include <string.h>


int main(int argc, char **argv) {
	
	//argv is first input, filename?
	
	int i;
	while (fscanf(argv[0], "%d\t", &array[i][j]) != EOF) {
		
	}
	
	
	
	//recursively free the nodes
	return 1;
}
